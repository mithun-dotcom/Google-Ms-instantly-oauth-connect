services:
  - type: web
    name: instantly-oauth-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: ALLOWED_ORIGIN
        value: "*"
      - key: PORT
        value: 10000
